services:

  web:
    build: .
    restart: always
    ports:
      - 80:${UVICORN_PORT:-80}
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: 3
